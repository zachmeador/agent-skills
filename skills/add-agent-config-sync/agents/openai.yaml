interface:
  display_name: "Add Agent Config Sync"
  short_description: "Install cross-agent config and skill syncing"
  default_prompt: "Use $add-agent-config-sync to make AGENTS.md and skills/ the canonical sources for this project."
